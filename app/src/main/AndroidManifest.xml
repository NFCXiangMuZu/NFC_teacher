<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.compaq.nfc_teacher"
    android:installLocation = "preferExternal"
    android:versionCode="1"
    android:versionName="1.0"

    >
    <uses-permission android:name="android.permission.ACCESS_BLUETOOTH_SHARE" />
    <uses-permission android:name="android.permission.NFC" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <uses-feature
        android:name="android.hardware.nfc"
        android:required="true" />

    <uses-sdk
        android:minSdkVersion="19"
        android:targetSdkVersion="19" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <service android:enabled="true" android:name="com.example.compaq.nfc_teacher.FileSendPercentService" />
        <service android:enabled="true" android:name="com.example.compaq.nfc_teacher.SendFileService" />

        <activity
            android:name="com.example.compaq.nfc_teacher.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.compaq.nfc_teacher.ListViewDB"
            android:label="@string/name_ListViewDB"
            >

        </activity>

        <activity
            android:name="com.example.compaq.nfc_teacher.CreateNameList"
            android:label="创建名单"
            >

        </activity>

        <activity
            android:name="com.example.compaq.nfc_teacher.NormalAttendence"
            android:label="正常签到"
            >

        </activity>

        <activity
            android:name="com.example.compaq.nfc_teacher.Reflect_information"
            android:label="@string/reflect_information_title"
            >

        </activity>

        <activity
            android:name="com.example.compaq.nfc_teacher.FileSend"
            android:label="文件发送"
            >

        </activity>

        <activity
            android:name="com.example.compaq.nfc_teacher.SelectFileActivity"
            android:label="选择文件"
            >

        </activity>

    </application>

</manifest>
